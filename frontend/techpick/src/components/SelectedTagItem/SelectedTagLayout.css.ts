import { style } from '@vanilla-extract/css';

export const selectedTagLayoutStyle = style({
  display: 'inline-flex',
  alignItems: 'center',
  maxWidth: `264px`,
  height: '20px',
  margin: 0,
  borderRadius: '4px',
});
